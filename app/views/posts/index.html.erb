<div class="all">
  <div class="bbb">
    <% @post.each do |post| %>
    <%= link_to post_path(post.id) do %>
      <div class="content_post" style="background-image: url(<%= post.image %>);" >
        <p><%= post.title %></p>
      </div> 
      <% end %>
    <% end %>
  </div>
</div>